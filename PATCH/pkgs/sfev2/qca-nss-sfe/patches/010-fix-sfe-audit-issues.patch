--- a/sfe_compat.h
+++ b/sfe_compat.h
@@ -21,7 +21,7 @@
  * ip_route_output - kernel 6.12 added 'scope' as 6th arg.
  * QSDK SFE calls with 5 args. Wrap to add RT_SCOPE_UNIVERSE.
  */
-#define ip_route_output(net, daddr, saddr, tos, oif) \
+#define sfe_ip_route_output(net, daddr, saddr, tos, oif) \
 	ip_route_output_compat(net, daddr, saddr, tos, oif)
 
 static inline struct rtable *ip_route_output_compat(struct net *net,
@@ -57,7 +57,10 @@ static inline struct rtable *ip_route_ou
  * Not present in dd-wrt kernel. vlan_dev_real_dev() goes to the
  * underlying real device, functionally equivalent for single-level VLAN.
  */
-#define vlan_dev_next_dev(dev) vlan_dev_real_dev(dev)
+static inline struct net_device *vlan_dev_next_dev(const struct net_device *dev)
+{
+	return vlan_dev_priv(dev)->real_dev;
+}
 
 /*
  * netif_is_gre / netif_is_ip6gre
@@ -117,7 +120,9 @@ static inline bool dev_fast_xmit(struct
 	if (likely(dev->priv_flags & IFF_XMIT_DST_RELEASE)) {
 		skb_dst_drop(skb);
 	} else {
-		skb_dst_force(skb);
+		if (unlikely(!skb_dst_force(skb))) {
+			goto fail;
+		}
 	}
 
 	queue_index = netdev_pick_tx(dev, skb, NULL);
--- a/sfe_ipv4.c
+++ b/sfe_ipv4.c
@@ -1510,15 +1510,15 @@ int sfe_ipv4_recv(struct net_device *dev
 	/*
 	 * Handle PPPoE bridge packets using 3-tuple acceleration if SFE_PPPOE_BR_ACCEL_MODE_EN_3T
 	 */
+#ifndef SFE_OPENWRT_COMPAT
 	if (unlikely(sfe_l2_parse_flag_check(l2_info, SFE_L2_PARSE_FLAGS_PPPOE_INGRESS)) &&
 	    unlikely(sfe_pppoe_get_br_accel_mode() == SFE_PPPOE_BR_ACCEL_MODE_EN_3T)) {
 		struct ethhdr *eth = eth_hdr(skb);
-#ifndef SFE_OPENWRT_COMPAT
 		if (!sfe_pppoe_mgr_find_session(l2_info->pppoe_session_id, eth->h_source)) {
 			return sfe_ipv4_recv_pppoe_bridge(si, skb, dev, len, iph, ihl, l2_info);
 		}
-#endif
 	}
+#endif
 
 	protocol = iph->protocol;
 	if (IPPROTO_UDP == protocol) {
--- a/sfe_ipv6.c
+++ b/sfe_ipv6.c
@@ -1236,15 +1236,15 @@ int sfe_ipv6_recv(struct net_device *dev
 	/*
 	 * Handle PPPoE bridge packets using 3-tuple acceleration if SFE_PPPOE_BR_ACCEL_MODE_EN_3T
 	 */
+#ifndef SFE_OPENWRT_COMPAT
 	if (unlikely(sfe_l2_parse_flag_check(l2_info, SFE_L2_PARSE_FLAGS_PPPOE_INGRESS)) &&
 	    unlikely(sfe_pppoe_get_br_accel_mode() == SFE_PPPOE_BR_ACCEL_MODE_EN_3T)) {
 		struct ethhdr *eth = eth_hdr(skb);
-#ifndef SFE_OPENWRT_COMPAT
 		if (!sfe_pppoe_mgr_find_session(l2_info->pppoe_session_id, eth->h_source)) {
 			return sfe_ipv6_recv_pppoe_bridge(si, skb, dev, len, iph, ihl, l2_info);
 		}
-#endif
 	}
+#endif
 
 	if (IPPROTO_UDP == next_hdr) {
 		return sfe_ipv6_recv_udp(si, skb, dev, len, iph, ihl, sync_on_find, l2_info, tun_outer, is_frag, fhdr);
--- a/sfe_ipv4_udp.c
+++ b/sfe_ipv4_udp.c
@@ -745,7 +745,7 @@ int sfe_ipv4_recv_udp(struct sfe_ipv4 *s
 		struct rtable *rt;
 		dst = dst_cache_get(&cm->dst_cache);
 		if (unlikely(!dst)) {
-			rt = ip_route_output(&init_net, ip_hdr(skb)->daddr, 0, 0, 0);
+			rt = sfe_ip_route_output(&init_net, ip_hdr(skb)->daddr, 0, 0, 0);
 			if (IS_ERR(rt)) {
 				DEBUG_TRACE("%px: VXLAN-GPE failed to get IPv4 route: %pI4 \n", skb, &ip_hdr(skb)->daddr);
 				rcu_read_unlock();
--- a/sfe_ipv4_tcp.c
+++ b/sfe_ipv4_tcp.c
@@ -906,7 +906,7 @@ int sfe_ipv4_recv_tcp(struct sfe_ipv4 *s
 		struct rtable *rt;
 		dst = dst_cache_get(&cm->dst_cache);
 		if (unlikely(!dst)) {
-			rt = ip_route_output(&init_net, ip_hdr(skb)->daddr, 0, 0, 0);
+			rt = sfe_ip_route_output(&init_net, ip_hdr(skb)->daddr, 0, 0, 0);
 			if (IS_ERR(rt)) {
 				DEBUG_TRACE("%px: VXLAN-GPE failed to get IPv4 route: %pI4 \n", skb, &ip_hdr(skb)->daddr);
 				rcu_read_unlock();
