# =============================================================================
# SELF/X86 网络映射示例（示例文件，不随 firmware 分发）
#
# 用途：在目标虚拟机首次启动时，/etc/uci-defaults/99-x86-vm-network 会读取
#       /etc/network.env，用 WAN_MAC / LAN_MAC 做稳定的网卡映射，适用于
#       Hyper-V / KVM 中网卡枚举顺序不稳定的场景。
#
# 用法：
#   1. 复制本文件到目标机器：cp network.env.example /etc/network.env
#   2. 按需修改为真实网卡 MAC（小写 xx:xx:xx:xx:xx:xx）。
#   3. 仅对全新安装且无既有 lan/wan 配置时生效；不会覆盖已有网络配置。
#
# 注意：
#   - 本文件仅为示例，SELF 不会把它复制进 firmware。
#   - 不要提交真实用户 MAC。
#   - 若显式指定的 MAC 在系统中找不到，脚本不会静默回退到其它网卡。
# =============================================================================
WAN_MAC="00:11:22:33:44:55"
LAN_MAC="00:11:22:33:44:66"