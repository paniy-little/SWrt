# SELF/X86：扩大本机 ephemeral port range（低风险容量优化）
# 作用对象：OpenClash outbound TCP/UDP、DDNS、下载器、路由器本机主动连接。
# 不是纯 NAT PPS 优化；不预分配巨大内存、不增加 backlog、不增加 busy polling。
# 本轮只加入这一项，不入列 tcp_tw_reuse / tcp_abort_on_overflow / 巨大 syn_backlog。
net.ipv4.ip_local_port_range = 10240 65535